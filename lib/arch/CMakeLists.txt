
#AUX_SOURCE_DIRECTORY(${CMAKE_CURRENT_SOURCE_DIR} ZEN3_SRCS)
ADD_SUBDIRECTORY(zen3)

FILE(GLOB ARCH_COMMON_SRCS "*.cc")

SET(ARCH_SRCS ${ARCH_COMMON_SRCS} ${ZEN3_SRCS})

SET(ARCH_SRCS ${ARCH_SRCS} PARENT_SCOPE)

#MESSAGE(STATUS "All Arch srcs" ${ARCH_SRCS})

